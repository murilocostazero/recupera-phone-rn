import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    card: {
        backgroundColor: '#FFF',
        padding: 8,
        marginVertical: 4,
        borderRadius: 8
    }
});

export default styles;